import Image from "next/image";
import { Instagram, Mail, MapPin, Phone } from "lucide-react";
import type { SiteContentShape } from "@/lib/defaultContent";
import { Reveal } from "@/components/ui/Reveal";
import { MagneticButton } from "@/components/ui/MagneticButton";
import { AbstractVortex } from "@/components/visual/AbstractVortex";

type ContactContent = SiteContentShape["contact"];
const iconByType = { email: Mail, phone: Phone, location: MapPin, instagram: Instagram } as const;

export function ContactSection({ content }: { content: ContactContent }) {
  const [nameField, emailField, projectField, messageField] = content.fields;
  return <section className="glass-panel p-6 md:p-10 lg:p-12"><div className="absolute inset-0 opacity-35"><Image src="/images/hero-portrait.jpg" alt="Contact background" fill className="object-cover object-[center_42%]" /><div className="absolute inset-0 bg-gradient-to-r from-bg via-bg/88 to-bg/55" /><div className="absolute inset-0 bg-gradient-to-t from-bg via-transparent to-bg/55" /></div><Reveal className="relative z-10 grid gap-10 lg:grid-cols-[55%_45%]"><div><p className="text-xs font-semibold uppercase tracking-[0.26em] text-neon">{content.label}</p><h1 className="mt-4 text-[44px] font-semibold leading-[1] tracking-[-0.045em] md:text-[64px]">{content.titleLine1}<br /><span className="text-gradient-purple">{content.titleHighlight}</span></h1><p className="mt-5 max-w-sm leading-7 text-white/62">{content.description}</p><form className="mt-8 grid gap-4"><div className="grid gap-4 md:grid-cols-2"><input className="rounded-2xl border border-white/10 bg-white/[0.045] px-5 py-4 text-sm outline-none transition placeholder:text-white/35 focus:border-neon/60" placeholder={nameField} /><input className="rounded-2xl border border-white/10 bg-white/[0.045] px-5 py-4 text-sm outline-none transition placeholder:text-white/35 focus:border-neon/60" placeholder={emailField} /></div><input className="rounded-2xl border border-white/10 bg-white/[0.045] px-5 py-4 text-sm outline-none transition placeholder:text-white/35 focus:border-neon/60" placeholder={projectField} /><textarea className="min-h-[150px] resize-none rounded-2xl border border-white/10 bg-white/[0.045] px-5 py-4 text-sm outline-none transition placeholder:text-white/35 focus:border-neon/60" placeholder={messageField} /><MagneticButton href="/contact" variant="solid" className="justify-center">{content.buttonText}</MagneticButton></form></div><div className="relative flex flex-col justify-center gap-4">{content.cards.map((item) => {const Icon = iconByType[item.type as keyof typeof iconByType] || Mail; return <div key={item.label} className="glass-card flex items-center gap-4 p-5"><span className="grid h-12 w-12 place-items-center rounded-2xl border border-neon/35 bg-neon/15 text-neon"><Icon size={22} /></span><div><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-white/50">{item.value}</p></div></div>;})}<div className="mt-6 grid place-items-center"><AbstractVortex className="h-44 w-44" /></div></div></Reveal></section>;
}
